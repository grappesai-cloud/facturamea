---
import AdminLayout from '../../layouts/AdminLayout.astro';
import { db } from '../../db';
import { broadcasts, users } from '../../db/schema';
import { sql, desc, eq } from 'drizzle-orm';

async function safe<T>(p: Promise<T>, fb: T) { try { return await p; } catch { return fb; } }

const recent = await safe(
  db.select({
    id: broadcasts.id, title: broadcasts.title, segment: broadcasts.segment,
    sendEmail: broadcasts.sendEmail, recipientsCount: broadcasts.recipientsCount,
    sentAt: broadcasts.sentAt, sentByName: users.name,
  }).from(broadcasts).leftJoin(users, eq(broadcasts.sentBy, users.id))
    .orderBy(desc(broadcasts.sentAt)).limit(20),
  []
);

const segments = [
  { value: 'all',           label: 'Toţi utilizatorii' },
  { value: 'transportator', label: 'Doar transportatori' },
  { value: 'intermediar',   label: 'Doar intermediari / expeditori' },
  { value: 'client_direct', label: 'Doar clienţi direcţi' },
  { value: 'partener',      label: 'Doar parteneri' },
  { value: 'tier:premium',  label: 'Abonament Premium' },
  { value: 'tier:cargo',    label: 'Abonament Cargo' },
  { value: 'tier:sprinter', label: 'Abonament Sprinter' },
];

const inputCls = 'w-full px-4 py-2.5 bg-white border border-[#E3EAF1] rounded-xl text-[13px] focus:border-[#07090f] focus:outline-none transition-colors';
const fmt = (d: any) => d ? new Date(d).toLocaleString('ro-RO') : '—';
---

<AdminLayout title="Broadcast" activeTab="broadcast">
  <div class="mb-6">
    <h1 class="text-[28px] sm:text-[36px] font-semibold tracking-[-0.03em] text-white">Broadcast notificări</h1>
    <p class="text-[14px] text-[#A8BED2] mt-1">Trimite o notificare în-app (şi opţional email) către un segment de utilizatori.</p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-3">
    <form id="broadcast-form" class="lg:col-span-2 space-y-4 p-5 sm:p-6 rounded-2xl bg-white/5">
      <div id="broadcast-banner" class="hidden items-start gap-2 px-4 py-3 rounded-xl text-[13px]"></div>
      <div>
        <label class="block text-[12px] font-medium text-[#A8BED2] mb-1.5">Segment</label>
        <select name="segment" required class={inputCls}>
          {segments.map((s) => <option value={s.value}>{s.label}</option>)}
        </select>
        <p id="recipient-preview" class="text-[11px] text-[#8FA6BC] mt-1.5">Estimare destinatari: —</p>
      </div>
      <div>
        <label class="block text-[12px] font-medium text-[#A8BED2] mb-1.5">Titlu (max 200)</label>
        <input name="title" required maxlength="200" placeholder="ex. Mentenanţă programată sâmbătă" class={inputCls} />
      </div>
      <div>
        <label class="block text-[12px] font-medium text-[#A8BED2] mb-1.5">Mesaj</label>
        <textarea name="body" required rows="6" placeholder="Conţinut mesaj — markdown simplu OK" class={inputCls + ' resize-y'}></textarea>
      </div>
      <div>
        <label class="flex items-center gap-2 text-[13px] text-[#D7E5F0]">
          <input type="checkbox" name="sendEmail" class="w-4 h-4 accent-[#34A0A4]" />
          <span>Trimite şi email (Resend)</span>
        </label>
      </div>
      <div class="pt-2">
        <button type="submit" id="send-btn" class="px-6 py-3 bg-[#E1FB15] hover:bg-[#D2EA0E] disabled:opacity-50 text-[#07090f] font-bold rounded-full text-[13.5px] transition-colors">
          Trimite broadcast
        </button>
        <p class="text-[11px] text-[#8FA6BC] mt-2">⚠️ Acţiunea e ireversibilă şi notifică oameni reali.</p>
      </div>
    </form>

    <div class="rounded-2xl bg-white/5 overflow-hidden h-fit">
      <div class="px-5 py-4 border-b border-white/10"><p class="text-[13px] font-semibold text-white">Istoric (ultimele 20)</p></div>
      <div class="divide-y divide-white/5 max-h-[600px] overflow-y-auto">
        {recent.length === 0 ? (
          <div class="px-5 py-10 text-center text-[13px] text-[#8FA6BC]">Fără broadcast-uri</div>
        ) : recent.map((b) => (
          <div class="px-5 py-3.5">
            <p class="text-[13px] font-semibold text-white truncate">{b.title}</p>
            <p class="text-[11px] text-[#8FA6BC] mt-1">
              {b.segment} · {b.recipientsCount ?? '?'} destinatari{b.sendEmail ? ' · email' : ''}
            </p>
            <p class="text-[10px] text-[#5E7385] mt-1">{fmt(b.sentAt)} · {b.sentByName ?? 'admin'}</p>
          </div>
        ))}
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('broadcast-form') as HTMLFormElement;
    const btn = document.getElementById('send-btn') as HTMLButtonElement;
    const banner = document.getElementById('broadcast-banner')!;
    const preview = document.getElementById('recipient-preview')!;

    async function refreshPreview() {
      const seg = (form.elements.namedItem('segment') as HTMLSelectElement).value;
      try {
        const res = await fetch(`/api/admin/broadcast?preview=1&segment=${encodeURIComponent(seg)}`);
        const data = await res.json();
        preview.textContent = `Estimare destinatari: ${data.count?.toLocaleString() ?? '—'}`;
      } catch { preview.textContent = 'Estimare destinatari: —'; }
    }
    (form.elements.namedItem('segment') as HTMLSelectElement | null)?.addEventListener('change', refreshPreview);
    refreshPreview();

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const fd = new FormData(form);
      const body = {
        segment: fd.get('segment'),
        title: fd.get('title'),
        body: fd.get('body'),
        sendEmail: fd.get('sendEmail') === 'on',
      };
      const ok = confirm(`Confirmă trimiterea către segmentul "${body.segment}"?`);
      if (!ok) return;
      btn.disabled = true; btn.textContent = 'Se trimite...';
      banner.classList.add('hidden');
      try {
        const res = await fetch('/api/admin/broadcast', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(body),
        });
        const data = await res.json();
        if (!res.ok) {
          banner.className = 'flex items-start gap-2 px-4 py-3 rounded-xl text-[13px] bg-[#DC4B41]/15 text-[#DC4B41]';
          banner.textContent = data.error || 'Eroare';
        } else {
          banner.className = 'flex items-start gap-2 px-4 py-3 rounded-xl text-[13px] bg-[#2E9E6A]/15 text-[#2E9E6A]';
          banner.textContent = `Trimis către ${data.recipientsCount} utilizatori.`;
          form.reset();
          setTimeout(() => window.location.reload(), 1500);
        }
      } catch (err) {
        banner.className = 'flex items-start gap-2 px-4 py-3 rounded-xl text-[13px] bg-[#DC4B41]/15 text-[#DC4B41]';
        banner.textContent = 'Eroare reţea';
      } finally {
        btn.disabled = false; btn.textContent = 'Trimite broadcast';
      }
    });
  </script>
</AdminLayout>
