---
// Client requests — accountant↔client document/info inbox.
import AppLayout from '../../../layouts/AppLayout.astro';
import RequestsManager from '../../../components/cereri/RequestsManager.tsx';

const user = Astro.locals.user!;
if (!user.companyId) return Astro.redirect('/app/setari');
---

<AppLayout title="Cereri documente" activeTab="cereri">
  <div class="mb-6">
    <h1 class="text-[30px] sm:text-[38px] font-semibold tracking-[-0.03em] text-white">Cereri documente</h1>
    <p class="mt-1.5 text-[15px] text-[#7E97AC]">Cere documente și informații, fără WhatsApp. Clientul e notificat și răspunde aici.</p>
  </div>
  <RequestsManager client:load />
</AppLayout>
