---
// Cereri documente — accountant↔client document/info inbox.
// The RequestsManager component fetches/manages its own data via /api/cereri
// (GET list, POST create) and /api/cereri/[id] (PATCH respond / resolve).
import AppLayout from '../../layouts/AppLayout.astro';
import RequestsManager from '../../components/cereri/RequestsManager.tsx';

const user = Astro.locals.user!;
if (!user.companyId) return Astro.redirect('/app/setari');
---

<AppLayout title="Cereri documente" activeTab="cereri">
  <div class="mb-6 sm:mb-8">
    <h1 class="text-[30px] sm:text-[40px] font-semibold tracking-[-0.03em] text-white">Cereri documente</h1>
    <p class="text-[15px] text-[#A8BED2] mt-1.5 max-w-2xl">Cere documente și informații clienților, fără WhatsApp. Clientul e notificat și răspunde direct aici.</p>
  </div>

  <div class="max-w-3xl">
    <RequestsManager client:load />
  </div>
</AppLayout>
