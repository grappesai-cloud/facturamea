{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "buildCommand": "astro build",
  "installCommand": "npm install",
  "regions": ["fra1"],
  "_note": "facturamea is deployed on Coolify (DEPLOY_TARGET=node), NOT Vercel. Cron jobs are Coolify Scheduled Tasks — see infra/CRON_SETUP.md. The previous Vercel `crons` block here never ran on Coolify and was removed."
}
